---
import SectionHeader from "./SectionHeader.astro";

interface Props {
  heading: string;
  sub?: string;
  surface?: boolean;
  id?: string;
  cols?: 2 | 3;
}

const { heading, sub, surface = false, cols = 3, id } = Astro.props;

const gridCols =
  cols === 2
    ? "grid grid-cols-2 gap-5 mt-10 max-md:grid-cols-1"
    : "grid grid-cols-3 gap-5 mt-10 max-md:grid-cols-2 max-sm:grid-cols-1";

const hasSubSlot = Astro.slots.has("sub");
---
<section id={id} class={`py-xl scroll-mt-[60px] max-sm:py-lg ${surface ? "bg-bg-surface" : ""}`}>
  <div class="mx-auto max-w-[1100px] px-md">
    {hasSubSlot ? (
      <SectionHeader heading={heading}>
        <slot name="sub" />
      </SectionHeader>
    ) : (
      <SectionHeader heading={heading} sub={sub} />
    )}
    <div class={gridCols}>
      <slot />
    </div>
  </div>
</section>
